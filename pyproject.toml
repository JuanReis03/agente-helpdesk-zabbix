[project]
name = "projeto-agente"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Juan", email = "juanreis07@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "langchain>=1.3.15",
    "ollama>=0.6.2",
    "reportlab>=5.0.0",
    "requests>=2.34.2",
    "uvicorn>=0.52.4",
]

[project.scripts]
projeto-agente = "projeto_agente:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
